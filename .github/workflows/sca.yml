name: Static Code Analysis

on: [pull_request]

jobs:
    run-sca:
        runs-on: ubuntu-latest
        steps:
            - name: Copy the Repo
              uses: actions/checkout@v1
            - name: Checking source code
              run: |
                python3 --version
                pip3 --version
                               
                
