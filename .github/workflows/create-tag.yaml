name: "My tag workflow"

on: 
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
    types: ["closed"]
    
    
jobs:
  print:
    runs-on: ubuntu-latest
    steps:
      - name: Curl
        run: |
          curl -o out.html www.google.com/linux --fail
          
  
